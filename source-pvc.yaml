apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: source-pvc
  namespace: devopsdays
spec:
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  storageClassName: premium-rwo 
  accessModes:
    - ReadWriteOnce