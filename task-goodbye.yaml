apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: goodbye
  namespace: kcn-mi
spec:
  steps:
    - name: echo
      image: ubuntu
      command:
        - echo
      args:
        - "Goodbye World !"
